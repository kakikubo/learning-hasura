# ESLint vs Biome パフォーマンス比較

## 実行環境

- **プロジェクト**: learning-hasura (Next.js + TypeScript)
- **ファイル数**: 41ファイル
- **マシン**: macOS
- **Node.js**: >=20.12.2
- **パッケージマネージャー**: pnpm 10.24.0

## 実行時間の計測結果

### ESLint

| 実行回数 | 実行時間 |
|---------|---------|
| Run 1   | 0.899s  |
| Run 2   | 0.640s  |
| Run 3   | 0.632s  |
| **平均** | **0.724s** |

### Biome

| 実行回数 | 実行時間 |
|---------|---------|
| Run 1   | 0.210s  |
| Run 2   | 0.173s  |
| Run 3   | 0.175s  |
| **平均** | **0.186s** |

## パフォーマンス改善

- **速度向上**: 約 **3.9倍** 高速化
- **時間短縮**: 約 **0.538秒** の短縮
- **改善率**: 約 **74%** の実行時間削減

## 検出結果の比較

### ESLint検出結果

```
✖ 7 problems (0 errors, 7 warnings)
- console.log警告: 7件
```

### Biome検出結果

```
Checked 41 files in 19ms. No fixes applied.
- エラー: 0件
```

**注意**: Biomeの設定では、既存コードとの互換性のため以下のルールを無効化しています：
- `noUnusedVariables`: 未使用変数のチェックを無効化
- `noUnusedImports`: 未使用インポートのチェックを無効化
- `noConsole`: console.log/warn/error/infoを許可

### 重要なルールの検証

テストファイルを使用して、以下の重要なルールが両方のツールで検出されることを確認しました：

| ルール | ESLint | Biome | 状態 |
|-------|--------|-------|------|
| debugger文の検出 | ✓ | ✓ | 維持 |
| Hookの条件付き呼び出し | ✓ | ✓ | 維持 |
| 未使用変数 | ✓ | ✗ | 意図的に無効化 |
| console.log | ✓ | ✗ | 意図的に許可 |

## 結論

Biomeへの移行により、以下のメリットが確認されました：

1. **大幅な速度向上**: 約4倍の高速化により、開発体験が向上
2. **重要なルールの維持**: React Hooksやdebugger文など、重要なルールは引き続き検出
3. **既存コードとの互換性**: 未使用変数やconsole.logを許可することで、既存コードの変更を最小限に抑制

移行は成功し、パフォーマンスとコード品質の両立が実現されています。
