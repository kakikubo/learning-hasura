# Hasura GraphQL Endpoint Configuration
# Copy this file to .env.local and update with your actual values

# Hasura GraphQL endpoint URL
NEXT_PUBLIC_HASURA_URL=https://your-hasura-instance.hasura.app/v1/graphql

# Hasura admin secret key
NEXT_PUBLIC_HASURA_KEY=your-admin-secret-key-here